---
name: voxeasy
description: VoxEasy — Vox 风格文生视频导演技能。接收一句话主题、完整文案或 SRT，先按语义和完整时间轴合并字幕并向上吸附为 4/6/8/10 秒 Shot；字幕分镜确认后，必须先选择模型、画面比例和视觉风格，再设计画面并确认，最后输出 Google Flow 可用的视频 Prompt。标准模式直接使用保存的原始 VoxEasy 4.0 视觉规则；美漫、纪念碑谷式柔和几何、复古报纸和自定义风格使用各自独立的画面设计流程。
---

# VoxEasy 4.9

严格执行：**先字幕分 Shot，再选择模型、比例与风格，后视觉设计，最后 Prompt**。

## 固定流程

1. 接收一句话主题、完整文案或 SRT。
2. 按语义关系和完整时间区间拆分或合并字幕，向上吸附为 `4 / 6 / 8 / 10` 秒 Shot。
3. 第一次确认：字幕文字、Shot 边界、原始时间和吸附时长。
4. 参数选择：让用户明确选择生成模型、画面比例和视觉风格。
5. 根据已确认字幕和已选择参数设计逐 Shot 画面。
6. 第二次确认：构图、元素、可见文字、动作、视角和运镜。
7. 两次确认及参数选择全部完成后才输出 Google Flow Prompt 与 JSON。

交互模式必须在字幕确认、参数选择和视觉确认三个节点停止。无头模式只有在输入明确包含相应的批准状态与参数时才能跳过。

---

## 阶段一：接收内容

### 一句话主题

- 生成完整、连贯、适合口播的字幕文案和候选标题。
- 涉及时效性事实或数据时先搜索验证，禁止编造。

### 完整文案

- 默认保留原文，不擅自改写事实、措辞和语气。
- 只进行语义分组和时长估算；需要改写时先提出建议。

### SRT 字幕

- 读取每条字幕的开始时间、结束时间和文字。
- 原时间轴为最高优先级，不得把字幕统一移到零点，也不得删除任何已知空白时间。
- SRT 模式不得自动在开头插入额外标题时长。若用户要求独立标题镜头，必须确认标题占用原时间轴的哪个区间，或确认整体字幕顺延。

`9:16`、Google Flow / Omni Flash、标准模式只是推荐默认值。交互模式不得把默认值视为用户已经选择。

---

## 阶段二：语义合并、完整时间与时长吸附

### 语义优先

- 相邻字幕表达同一主体、同一动作、同一因果链或同一结论时，可以合并。
- 主题、主体、时间、立场或叙事功能明显变化时必须拆开。
- 不为了填满 10 秒合并无关内容，也不因为单句较短就机械地一行一个 Shot。
- 合并后的完整时间区间不得超过 10 秒；超过时在最近的自然语义边界拆开。

### SRT 完整时间算法

Shot 时长按连续时间区间计算，禁止把各条字幕的发声时长简单相加：

```text
actual_duration = shot_end - shot_start
```

必须计入：

- 第一条字幕前从 `00:00.000` 开始的开场空白。
- 同一 Shot 内多条字幕之间的全部停顿。
- 两个 Shot 之间的空白；默认归入前一个 Shot，作为画面保持时间。
- 已知媒体总时长时，最后一条字幕之后的尾部空白。

如果把间隔归入前一个 Shot 会导致其超过 10 秒，则可把该间隔归入下一个 Shot，但必须在确认表中明确标出，任何空白都不能消失。

示例：

```text
字幕 1：00:00.800 → 00:03.100
字幕 2：00:03.600 → 00:06.700
合并区间：00:00.000 → 00:06.700
实际时长：6.7 秒
包含空白：开场 0.8 秒 + 中间 0.5 秒
吸附时长：8 秒
```

单条 SRT 超过 10 秒时，在语义边界拆分文字，并按文字长度与停顿合理分配原时间区间；把分配结果标为估算，交给用户确认。

### 普通文案时间

- 中文按每分钟 `220–240` 字估算，英文按每分钟 `130–150` 词估算。
- 标点和自然停顿计入口播时间。
- 同样先按语义合并，再向上吸附。

### 可长不可短

选择能够完整覆盖 `actual_duration` 的最小模型档位：

- `actual_duration ≤ 4s` → `4-second duration`
- `4s < actual_duration ≤ 6s` → `6-second duration`
- `6s < actual_duration ≤ 8s` → `8-second duration`
- `8s < actual_duration ≤ 10s` → `10-second duration`
- `actual_duration > 10s` → 重新拆分

模型素材允许比实际字幕区间长，后期可以裁掉尾部；绝对不能选择比实际区间短的档位。

### 标题镜头

- 非 SRT 模式默认提供独立 Shot 01 标题镜头，时长 4 秒，`voiceover_text` 为空。
- SRT 模式服从原时间轴，不自动增加或移动标题时长。

### 第一次确认

只输出：

| Shot | 原始时间范围 | 字幕内容 | 字/词数 | 内含空白 | 实际时长 | 吸附时长 |
|---|---|---|---:|---:|---:|---:|

然后停止。用户确认前不得设计画面或生成 Prompt。

---

## 阶段三：选择生成参数

字幕分镜确认后、设计画面前，必须用一个紧凑选择块让用户明确选择：

- **生成模型**：推荐 `Google Flow / Omni Flash`；也可由用户直接指定其他模型。不要自行扩展固定模型清单。
- **画面比例**：根据已选模型显示其实际支持的比例，不得提供模型不支持的选项。
  - `Google Flow / Omni` 只提供 `9:16`（推荐）和 `16:9`，不得提供或传入 `1:1`。
  - 其他模型只有在已确认原生支持时才能提供 `1:1` 或其他比例；无法确认时先询问，不得猜测。
- **视觉风格**：`标准 Vox 4.0`（推荐）、`美漫`、`纪念碑谷式柔和几何`、`复古报纸`、`自定义`。

然后停止，等待用户选择。不得在用户明确选择前设计画面。

- 用户已经在当前输入中明确指定的参数直接记录，只询问尚未指定的项目。
- 同一条视频默认统一使用一个比例和一种视觉风格。
- 只有用户明确要求混合风格时，才允许逐 Shot 选择风格，并在选择表中列出对应关系。
- 用户改变参数时，只作废尚未确认或已经确认的视觉设计；已确认的字幕分镜与原始时间轴保持不变。

### Flow 方形成片

用户要求 `1:1` 且目标为 Google Flow / Omni 时：

1. 明确说明模型不原生支持 `1:1`，不得生成或声称生成方形素材。
2. 提供 `9:16` 中央方形安全区（推荐）或 `16:9` 中央方形安全区，让用户选择实际生成比例。
3. 用户确认前停止，不得自行替用户选择。
4. 把人物、主体、动作、标题和全部重要文字限制在画面中央的 `1:1` 裁切安全区；安全区外只延伸非关键背景。
5. Prompt 开头、结尾和生成接口始终填写实际比例，例如 `vertical 9:16` 与 `--ar 9:16`，绝不能写 `1:1`。
6. JSON 使用实际 `aspect_ratio`，并另写 `crop_target_ratio: "1:1"`。后期从中央裁切方形成片。

参数选择完成后，先回显：

```text
模型：...
实际生成比例：...
裁切目标：无 / 1:1
风格：...
```

再进入视觉设计。

---

## 阶段四：按风格独立设计画面

只根据已经确认的字幕 Shot 设计画面。

### 标准模式

- 完整读取 [`references/vox-standard-v4.0.md`](references/vox-standard-v4.0.md)。
- 直接使用其中原始 4.0 的视觉设计、纸质拼贴、实体映射、配色、质感、运镜和 Prompt 输出规则。
- 不使用任何历史生成示例作为通用模板；工作台、蓝图、卡片、云门等具体元素都必须由当前字幕决定，不能固定加入。
- 不增加美漫、纪念碑谷式、报纸或其他后续画风词。
- 原始参考文件保持逐字不变。执行时只由当前文件覆盖它的旧输入流程、确认顺序、目标引擎比例可用性、时长阈值和动作时间戳；其余标准视觉与 Prompt 规则原样执行。

### 扩展风格

用户选择美漫、纪念碑谷式、复古报纸或自定义风格时，完整读取 [`references/style-presets.md`](references/style-presets.md)。

- 美漫使用自己的分格、冲击画面、图解、网点、排线和镜头语言。
- 纪念碑谷式使用自己的几何空间、平衡构图、长阴影和空间运动。
- 复古报纸使用自己的头版、档案拼贴、栏目、印刷结构和版面运动。
- 自定义风格根据用户描述建立独立的构图、材质、动作与镜头规则。

扩展风格不套用标准模式的画面构图，只共用已经确认的字幕含义、完整时间和模型时长。

扩展风格只改变画面设计语言，不得降低 Prompt 精度。每个扩展风格 Prompt 都必须达到原始 4.0 的信息完整度：明确写出实际比例、吸附时长、全画幅视觉基底、实体映射、主体与辅助元素的造型／颜色／位置、前中后景层次、准确文字白名单、分时动作、连续运镜、材质与动画锁定词、负面约束和最终 `--ar`。具体顺序与风格专属结尾完整读取 `references/style-presets.md`，不得只输出风格关键词加简略剧情。

### 第二次确认

逐 Shot 输出：

- 核心画面
- 主体与辅助元素
- 允许显示的准确文字
- 动作节拍
- 视角与运镜
- `visual_style`

然后停止。用户确认前不得输出最终英文 Prompt。

---

## 阶段五：动作与运镜

每个 Shot 只使用一个核心视觉隐喻和一条连续动作弧。按生成时长使用以下固定分段：

- 4 秒：`[0-2s]`、`[2-4s]`
- 6 秒：`[0-3s]`、`[3-6s]`
- 8 秒：`[0-4s]`、`[4-8s]`
- 10 秒：最多三段，`[0-3s]`、`[3-7s]`、`[7-10s]`

4/6/8 秒使用“建立初始状态 → 完成核心转变”；10 秒使用“建立 → 演变 → 结果”。若 10 秒用两段即可表达清楚，不得为了填满三段增加动作。

严格控制复杂度：

- 每个 Shot 只发生一次主要转变；不得按每句字幕或每个名词分别设计动作。
- 使用一个中心主体或系统；辅助元素合并为最多三组，并作为整体出现或移动。
- 每个时间段只描述一个主要可见变化，避免连续堆叠“展开、弹出、扫描、连接、盖章”等独立动作。
- 可见文字尽量不超过三组；用户确认的完整标题不受此限制。
- 运镜从头到尾连续执行，不作为额外动作节拍。
- 字幕包含多个并列项目时，先让这些项目共同存在于初始画面，再整体整理、汇聚或转变，不逐项登场。

标准模式的视角与运镜继续执行原始 4.0；扩展风格使用各自参考文件的镜头语言，不得互相混用。

---

## 阶段六：最终输出

完成两次确认及参数选择后输出：

1. 声画对照表。
2. 每个 Shot 一段 Google Flow 可直接复制的英文 Prompt。
3. 结构化 JSON。

每段最终 Prompt 的前两句必须同时出现实际比例和明确吸附时长，例如：

```text
[风格开头], vertical 9:16. 4-second duration.
```

时间戳只负责动作编排，不能代替时长参数。JSON 的 `duration_seconds` 必须等于该 Shot 的 `4 / 6 / 8 / 10` 秒吸附档位；如果同时生成 Flow 或其他视频工具调用，调用参数中的时长也必须与其完全一致。

JSON 至少包含：

```json
{
  "shot_id": "02",
  "source_start": "00:00.000",
  "source_end": "00:06.700",
  "subtitle_text": "确认后的字幕",
  "included_blank_seconds": 1.3,
  "actual_duration_seconds": 6.7,
  "duration_seconds": 8,
  "aspect_ratio": "9:16",
  "crop_target_ratio": null,
  "visual_style": "vox-standard",
  "visual_summary": "确认后的画面",
  "camera": "确认后的运镜",
  "visible_text": ["确认后的短标签"],
  "visual_prompt": "完整 Prompt"
}
```

输出前逐 Shot 检查：

1. 内容相近的字幕是否合理合并，内容转折处是否拆开？
2. SRT 是否保留了开场、内部、镜头间和已知尾部空白？
3. `actual_duration_seconds` 是否按完整时间区间计算？
4. `duration_seconds` 是否为不短于实际区间的最小 `4/6/8/10` 档位？
5. 字幕 Shot 是否已确认，模型、比例和风格是否已经由用户明确选择？
6. 视觉分镜是否已确认？
7. 标准模式是否只使用原始 4.0 视觉规则？
8. 扩展风格是否使用自己的画面设计流程，同时达到标准 4.0 的 Prompt 信息完整度？
9. Prompt 是否显式写出吸附时长，JSON 与生成调用的时长是否完全一致？
10. 主体、辅助元素、空间层次、文字白名单、动作、运镜和风格锁定词是否都已写实写全，而非只有抽象风格词？
11. 动作是否符合 2–3 段上限，并保持一个核心隐喻与一次主要转变？
12. 实际生成比例是否受目标模型支持，方形需求是否记录中央安全区和 `crop_target_ratio`？

任一项为“否”时不得输出最终 Prompt。
